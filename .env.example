# Hollywood Cut 配置环境变量示例
# 注意：本项目为纯前端 (Zero-Build) 架构。在当前形态下，无需配置本地环境变量。
# 用户直接通过网页 UI 输入此 API Key (存于 LocalStorage)。
# 若后期扩展后端或构建工具 (如 Vite)，请按此格式配置真实 .env 文件。

GEMINI_API_KEY=your_gemini_api_key_here
